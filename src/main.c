#include "puma.h"

//#define NX 1000
//#define NY 1000

int main()
{
	int nx, ny;
	int map[NX][NY] = {0};
	//readmap(map, "small.dat", &nx, &ny);
	printf("%d %d %d\n", map[0][0], map[49][49], map[5][5]);
	return 0;
}
